# 播放器耐久测试

KTVS-026 使用 `scripts/test-player-endurance.ps1` 重建 Unicode 双音轨/字幕 MKV，并在同一个 mpv 进程中重复执行加载、交替原唱/伴奏、字幕开关、定位到末段和自然结束。测试要求每个 `PlaybackId` 只收到一次完成事件、进程不重启、每轮状态回到 `Ended`，并把耗时和工作集写入忽略提交的 `TestResults/player-endurance-report.json`。

默认快速回归为 20 轮；2026-09-08 另执行 50 轮软件门禁，结果为 50/50 完成、重复结束事件 0、mpv 重启 0、耗时 32.339 秒。mpv 工作集从 33,775,616 字节增至 107,180,032 字节，列为后续长时实机观察项，短时增长本身不判定为泄漏。合成循环用于尽早发现 IPC、事件去重和进程生命周期回归，不能替代真实 50 首完整时长验收。

## 待实机验收：50 首

- [ ] 从真实 CloudDrive/115 挂载连续播放 50 首不同 MKV，不手工干预。
- [ ] 每首自然完成只推进一次，错误文件按策略重试后跳过，队列不停止。
- [ ] 至少每 5 首切换一次原唱/伴奏，每 10 首检查一次字幕开关。
- [ ] 电视无黑屏残留或窗口抢焦点，功放无异常爆音、静音或声道错乱。
- [ ] 记录开始/结束时间、失败歌曲、错误码、mpv/Station 内存和人工观察。

真实电视、功放和曲库结果保持“待实机验收”，不得用合成报告标记为通过。
