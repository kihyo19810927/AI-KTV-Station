# AI-KTV Station 完整开发计划书与 Codex 执行提示词

版本：1.0  
日期：2026-09-03  
项目代号：AI-KTV Station  
目标平台：Windows 11 家庭 KTV 主机 + 局域网手机点歌  
曲库来源：115 网盘，经 CloudDrive/CloudDrive2 挂载到 Windows  

---

## 1. 文档目的

本文既是产品与技术开发计划书，也是交给 Codex 持续执行的工作合同。它规定：

- 产品做什么、不做什么；
- 技术栈、架构边界与关键接口；
- 从空仓库到可用版本的任务顺序；
- 每个阶段的验收条件；
- Codex 可以自主决定的事项，以及必须暂停询问用户的事项；
- Git、测试、文档、发布和故障恢复规范；
- 可以直接复制给 Codex 的主提示词和后续提示词。

本项目与现有 `AI-KTV-Builder` 分开开发：

- `AI-KTV-Builder`：负责下载、音源分离、歌词转录/校正/对齐、字幕制作、MKV 封装；
- `AI-KTV Station`：负责曲库索引、搜索点歌、队列、手机控制和成品播放。

首版禁止将两者强耦合。未来只通过稳定的文件交付边界或显式 API 集成。

---

## 2. 项目目标

开发一套面向家庭和朋友聚会的 KTV 点歌系统：

1. Windows 电脑连接电视或投影仪、功放和麦克风设备；
2. Windows 主程序管理曲库、房间、播放和系统设置；
3. 朋友扫描二维码，用手机浏览器进入房间，不安装 App；
4. 手机可按歌名、歌手、拼音、首字母、语言和类别检索歌曲；
5. 多人可实时点歌、查看队列、删除自己的歌曲；
6. 主持人可置顶、切歌、暂停、重唱、切换原唱/伴奏和字幕；
7. 播放器直接读取 CloudDrive 挂载的 115 文件，不由 Web 服务转发整段视频；
8. 曲库即使达到数十万首，搜索和日常增量扫描仍保持可用；
9. 115 临时离线、403、路径变化或媒体损坏时，系统可以记录、提示并自动跳过；
10. 所有关键操作、扫描和播放错误可追踪、可恢复。

### 2.1 成功标准

V1.0 达到以下标准即视为首个正式可用版本：

- 可以扫描实际 115 挂载目录并建立本地索引；
- 可以稳定检索不少于 100,000 条曲目；
- Windows 主机能够连续播放至少 50 首测试歌曲而不崩溃；
- 能正确控制暂停、继续、切歌、重唱、音量、音轨和字幕；
- 至少 5 台手机可同时加入房间并实时同步队列；
- Windows 重启后数据库、配置、收藏和历史仍保留；
- CloudDrive 临时不可用时不会损坏数据库或卡死整个队列；
- 安装、初始化、备份和恢复都有操作文档。

---

## 3. 范围

### 3.1 V1.0 必须实现

- Windows 桌面主控程序；
- 本地 ASP.NET Core API；
- SignalR 实时消息；
- React 手机点歌页面；
- SQLite 曲库数据库；
- 多目录媒体源配置；
- MKV 扫描、增量扫描和离线标记；
- 文件名元数据解析；
- 拼音、首字母和原文搜索；
- mpv 播放控制；
- 原唱/伴奏和字幕轨切换；
- 点歌队列、房间、访客和主持人权限；
- 播放历史、收藏、热门歌曲；
- 二维码入房；
- 日志、错误诊断、数据库备份；
- Windows 安装包或可重复部署包。

### 3.2 V1.1 候选功能

- 下一首预读和可配置本地缓存；
- 歌曲海报或缩略图；
- 电视端更完整的“正在播放/下一首”叠加界面；
- 手机投票切歌；
- 公平轮唱模式；
- 多套播放列表和暖场模式；
- 批量修正歌曲资料；
- AI-KTV-Builder 完成后显式通知增量入库；
- 更精细的远端媒体健康检查；
- 歌词显示主题和字体配置。

### 3.3 暂不纳入 V1.0

- 麦克风实时混音、混响和反馈抑制；
- 专业硬件效果器控制；
- 演唱评分；
- 自动修音；
- 公网开放点歌；
- 多家庭、多租户 SaaS；
- 115、百度网盘的下载或迁移管理；
- 自动删除、移动或重命名实际媒体文件；
- 未经审查直接写入现有 AI-KTV-Builder 仓库；
- 绕过云盘访问控制、限流或 DRM。

---

## 4. 用户场景与角色

### 4.1 主持人

使用 Windows 主控台，可以：

- 创建或关闭 KTV 房间；
- 查看播放器与远端挂载状态；
- 搜索和点歌；
- 调整队列、置顶、切歌和重唱；
- 暂停、继续、调节音量；
- 切换伴奏、原唱和字幕；
- 管理曲库、触发扫描；
- 修改错误的歌曲信息或音轨映射；
- 查看播放和系统错误。

### 4.2 普通访客

扫描二维码进入手机页面，可以：

- 输入昵称加入房间；
- 搜索和筛选歌曲；
- 点歌和收藏；
- 查看当前歌曲和队列；
- 删除本人仍在等待的歌曲；
- 按房间规则调整本人歌曲；
- 发起切歌或原唱请求，但不能直接执行管理员操作。

### 4.3 系统维护者

- 配置媒体目录和 mpv/FFmpeg 路径；
- 备份与恢复 SQLite 数据库；
- 查看结构化日志；
- 导出曲库、播放历史和错误报告；
- 升级应用和执行数据库迁移。

---

## 5. 推荐技术栈

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 桌面主控 | C#、.NET 10、WPF、MVVM | Windows 原生集成、设置和主控界面 |
| 服务端 | ASP.NET Core Minimal API | 嵌入桌面进程，对手机提供 HTTP API |
| 实时通信 | ASP.NET Core SignalR | 当前播放、队列和房间状态实时同步 |
| 手机端 | React、TypeScript、Vite | 局域网页，适合手机浏览器，不需要 SSR |
| 数据访问 | EF Core + SQLite | 单机部署简单，支持迁移和事务 |
| 搜索 | SQLite FTS5 + 预计算规范化字段 | 歌名、歌手、拼音、简拼和原文检索 |
| 播放 | mpv 子进程 + JSON IPC | 稳定控制 MKV、音轨、字幕和播放状态 |
| 媒体探测 | ffprobe/FFmpeg | 读取容器、时长、音频轨和字幕轨信息 |
| 二维码 | 可离线生成二维码的 .NET 库 | 二维码只包含局域网地址和短期房间信息 |
| 日志 | `Microsoft.Extensions.Logging` + 结构化文件日志实现 | 日志轮转、诊断和导出 |
| 测试 | xUnit + FluentAssertions 等兼容工具 | 单元、集成、契约和端到端测试 |
| 打包 | `dotnet publish` + Windows 安装器 | 提供版本化、可升级的发布包 |

依赖必须选择仍受维护、许可证兼容且与目标 .NET 版本匹配的稳定版本，并提交锁定文件。不要只因为流行而引入大型框架。

### 5.1 为什么不用 Next.js

手机端是局域网单页应用，不需要 SEO、服务端渲染或云端边缘运行。React + Vite 构建出的静态文件直接由 ASP.NET Core 提供，部署和排错更简单。

### 5.2 为什么不用 Python 写主控

Python 继续负责 AI-KTV-Builder 很合适，但主控需要长期稳定地控制 Windows 窗口、显示器、音频设备、子进程和后台服务。用户熟悉 C#，因此 C#/.NET 是主系统更合适的核心。

### 5.3 为什么不把 PotPlayer 当正式引擎

PotPlayer可保留为人工验证工具，但正式系统需要可靠的机器控制协议。mpv 的 JSON IPC 更适合加载文件、获取事件、选择音轨、处理错误和自动下一首。播放器必须通过 `IPlayerAdapter` 抽象，未来可替换实现。

---

## 6. 总体架构

### 6.1 运行拓扑

```text
手机浏览器
    │ HTTP + SignalR（局域网）
    ▼
Windows AI-KTV Station
    ├── WPF 主控台
    ├── ASP.NET Core API/SignalR
    ├── 应用服务与队列状态机
    ├── SQLite 索引数据库
    ├── 曲库扫描器/ffprobe
    └── mpv JSON IPC
            │
            ▼
       电视/投影仪 + 音频输出

CloudDrive 挂载盘 ──> 扫描器和 mpv直接读取 ──> 115曲库
AI-KTV-Builder ──> 成品MKV目录 ──> 后续由显式扫描入库
```

### 6.2 单进程优先

V1.0 使用一个 Windows 主程序：

- WPF 启动时构建 .NET Generic Host；
- 在同一主程序中启动 ASP.NET Core/Kestrel；
- React 构建产物嵌入或复制到 `wwwroot`；
- mpv 作为受控子进程运行；
- 后台服务执行扫描、播放事件处理和健康检查。

这样用户只需启动一个程序。架构上仍保持模块分离，以便未来拆分 Windows Service。

### 6.3 关键边界

1. Web API 不传输整段视频，mpv直接读取媒体路径。
2. 手机端永远看不到 Windows 真实文件路径。
3. 扫描器只能读取媒体；V1.0 禁止移动、删除或重命名媒体。
4. 播放队列状态只由服务端写入，手机是命令发起者而不是权威状态源。
5. mpv事件必须转换为内部播放事件，业务层不得散落直接 IPC 调用。
6. 文件名解析与 NFO 读取是可替换的元数据提供器。
7. AI-KTV-Builder 与本项目不共享数据库，不进行隐藏调用。

---

## 7. 建议仓库结构

```text
AI-KTV-Station/
├── .github/
│   └── workflows/
├── docs/
│   ├── project/
│   │   ├── CHARTER.md
│   │   ├── STATUS.md
│   │   ├── BACKLOG.md
│   │   ├── DECISIONS.md
│   │   ├── TEST-MATRIX.md
│   │   └── RELEASE-CHECKLIST.md
│   ├── adr/
│   ├── api/
│   └── runbooks/
├── src/
│   ├── KtvStation.Domain/
│   ├── KtvStation.Application/
│   ├── KtvStation.Infrastructure/
│   ├── KtvStation.Server/
│   ├── KtvStation.Desktop/
│   └── KtvStation.Web/
├── tests/
│   ├── KtvStation.Domain.Tests/
│   ├── KtvStation.Application.Tests/
│   ├── KtvStation.Infrastructure.Tests/
│   ├── KtvStation.Server.Tests/
│   └── KtvStation.EndToEnd.Tests/
├── tools/
├── samples/
├── Directory.Build.props
├── Directory.Packages.props
├── AI-KTV-Station.slnx
├── AGENTS.md
└── README.md
```

### 7.1 依赖方向

```text
Domain <- Application <- Infrastructure
                    <- Server
                    <- Desktop
```

- `Domain` 不依赖 EF、WPF、ASP.NET 或 mpv；
- `Application` 定义用例和端口接口；
- `Infrastructure` 实现 SQLite、扫描器、文件系统、ffprobe 和 mpv；
- `Server` 提供 API、SignalR 和 Web 静态资源；
- `Desktop` 负责组合根、主控 UI 和 Windows 生命周期。

---

## 8. 核心领域模型

### 8.1 主要实体

| 实体 | 关键内容 |
| --- | --- |
| `Song` | 标题、规范化标题、语言、类别、年份、清晰度、可用状态 |
| `Artist` | 名称、规范化名称、拼音、简拼 |
| `SongArtist` | 歌曲与多位歌手的顺序和关系 |
| `MediaSource` | 本地或挂载根目录、启用状态、扫描策略 |
| `MediaFile` | 相对路径、大小、时间、探测信息、可用性 |
| `MediaTrack` | 视频、音频、字幕轨及语言和标题 |
| `TrackMapping` | 原唱、伴奏和默认字幕对应的轨道 ID |
| `RoomSession` | 房间、创建时间、状态、规则 |
| `Guest` | 昵称、房间令牌、加入时间、权限 |
| `QueueItem` | 歌曲、点歌人、顺序、状态、点歌时间 |
| `Favorite` | 访客或本地用户收藏关系 |
| `PlayHistory` | 播放结果、开始/结束时间、错误 |
| `ScanRun` | 扫描范围、进度、统计和错误摘要 |
| `PlaybackError` | 文件、错误码、阶段、可重试性、诊断信息 |

### 8.2 歌曲身份规则

- 数据库内部使用稳定 ID；
- 初始文件身份使用 `MediaSourceId + 规范化相对路径`；
- 文件大小和修改时间只用于变更检测，不单独作为身份；
- 可选的首尾分块指纹用于判断重命名或同名异内容，禁止默认计算整文件哈希，以免大量读取115；
- 文件离线时先标记，不立即删除数据库记录；
- 只有管理员明确执行“清理失效记录”才物理删除索引记录。

### 8.3 队列状态

```text
Waiting -> Preparing -> Playing -> Completed
                     ├-> Skipped
                     └-> Failed
Playing -> Paused -> Playing
```

状态迁移必须由服务端事务性执行，播放完成事件应具备幂等性，避免 mpv 重复事件导致连续跳过两首。

---

## 9. 曲库扫描与元数据

### 9.1 扫描流程

1. 读取启用的媒体源；
2. 枚举支持的扩展名，V1.0 默认 `.mkv`；
3. 忽略 `.downloading`、`.part`、`.tmp` 等临时文件；
4. 规范化相对路径和 Unicode；
5. 读取大小、修改时间和基本文件状态；
6. 新增或变化文件进入探测队列；
7. 文件名解析器生成候选元数据；
8. ffprobe 提取时长、轨道和编码信息；
9. 写入数据库；
10. 未再次发现的旧文件标记为离线；
11. 发布扫描进度和完成事件。

扫描必须：

- 可取消；
- 可恢复；
- 单个文件错误不终止全局扫描；
- 支持并发度配置，但对远端挂载默认低并发；
- 记录失败路径和原因；
- 不修改媒体文件；
- 支持全量和增量模式。

### 9.2 文件名解析

需要覆盖现有格式：

```text
马健涛-分手[1080P]-国语-流行.mkv
谢娜_A LIN黄丽玲_贾静雯-WHY OH WHY(繁体版)-国语-合唱.mkv
周杰伦-夜曲(MTV)-国语-流行歌曲.mkv
```

解析原则：

- 从右向左识别已知标签，避免歌名中的连字符被错误截断；
- 支持半角/全角括号、连接符和空格；
- `_` 可作为多歌手候选分隔符，但保留原始显示字符串；
- 解析结果保留置信度和警告；
- 解析失败时仍可入库，标题回退为不含扩展名的完整文件名；
- 手工修正字段优先于再次扫描结果；
- NFO 只作为可选元数据来源，文件名不一致不阻止入库。

### 9.3 搜索字段

每首歌预计算：

- 原始标题和歌手；
- Unicode/大小写/空白规范化文本；
- 简体和繁体兼容搜索键（实现前先ADR评估库）；
- 汉语拼音；
- 拼音首字母；
- 英文和数字紧凑形式；
- 多歌手的独立检索字段；
- 可选的日文假名/罗马字字段作为后续增强。

目标：在代表性 Windows 主机、100,000～300,000 条歌曲数据下，普通查询 P95 小于 200ms，不触碰媒体文件系统。

---

## 10. 播放子系统

### 10.1 播放器抽象

定义 `IPlayerAdapter`，至少提供：

- `StartAsync` / `StopAsync`；
- `LoadAsync`；
- `PlayAsync` / `PauseAsync`；
- `SeekAsync`；
- `SetVolumeAsync`；
- `SelectAudioTrackAsync`；
- `SelectSubtitleTrackAsync`；
- `GetStateAsync`；
- 播放开始、暂停、结束、错误和轨道变化事件。

业务层只能依赖该接口。mpv实现负责：

- 启动和监管子进程；
- 创建唯一 Named Pipe；
- JSON IPC 请求/响应关联；
- 超时、断线和重启；
- 解析 mpv 事件；
- 安全转义 Windows 和 Unicode 路径；
- 日志中隐藏不必要的本机敏感路径。

### 10.2 播放前检查

在歌曲进入 `Preparing` 后：

- 确认文件或挂载路径可访问；
- 使用限时检查，不能无限卡住队列；
- 确认 mpv 可加载；
- 应用保存的原唱/伴奏/字幕轨映射；
- 失败时记录明确原因并按策略重试或跳过；
- 下一首可在不大量下载文件的前提下进行轻量预检。

### 10.3 音轨映射

- 不假设所有 MKV 的音轨顺序一致；
- 优先使用轨道标题、语言和历史修正判断；
- 无法确定时显示“音轨1/音轨2”；
- 用户手动指定后保存到 `TrackMapping`；
- 批量规则必须先预览，不允许静默覆盖人工设置。

### 10.4 播放故障策略

- 第一次加载超时：短暂重试一次；
- 403/远端不可用：标记可重试错误并跳过，不删除歌曲；
- 文件不存在：标记离线；
- 解码错误：记录为媒体错误；
- mpv崩溃：自动重启一次并恢复队列；
- 连续失败达到阈值：暂停自动播放，提示主持人检查挂载。

---

## 11. 房间、队列和权限

### 11.1 房间

- 默认只允许一个活动房间；
- 房间具有短期 ID、显示名称和管理员令牌；
- 二维码不可包含文件系统路径或永久管理员密钥；
- 房间关闭后普通访客令牌失效；
- 局域网地址变化时可重新生成二维码。

### 11.2 队列规则

默认规则：

- 每位访客最多同时等待 5 首；
- 完全相同歌曲不可重复进入等待队列；
- 访客只能删除本人仍在等待的歌曲；
- 主持人可以删除、置顶、移动和插播；
- 当前歌曲切歌需要主持人确认，投票切歌留到 V1.1；
- 空队列时可停止或进入暖场列表；
- 所有排序变更通过服务端生成单调版本号。

### 11.3 SignalR事件

建议事件：

```text
RoomSnapshotChanged
QueueChanged
NowPlayingChanged
PlaybackProgressChanged
GuestJoined
GuestLeft
ScanProgressChanged
SystemHealthChanged
```

客户端断线重连后必须先获得完整快照，再接收增量事件，不能仅依赖丢失后无法恢复的推送。

---

## 12. API草案

### 12.1 公共/访客API

```text
POST   /api/rooms/{roomId}/join
GET    /api/rooms/{roomId}/snapshot
GET    /api/songs/search
GET    /api/songs/{songId}
POST   /api/queue
DELETE /api/queue/{queueItemId}
GET    /api/favorites
POST   /api/favorites/{songId}
DELETE /api/favorites/{songId}
```

### 12.2 主持人API

```text
POST   /api/admin/rooms
DELETE /api/admin/rooms/{roomId}
POST   /api/admin/queue/{id}/promote
POST   /api/admin/playback/skip
POST   /api/admin/playback/replay
POST   /api/admin/playback/pause
POST   /api/admin/playback/resume
POST   /api/admin/playback/audio-track
POST   /api/admin/playback/subtitle-track
POST   /api/admin/scans
POST   /api/admin/scans/{id}/cancel
GET    /api/admin/health
```

API必须有版本化策略、统一错误结构、输入验证、分页和取消支持。接口完成后生成 OpenAPI 文档，并用契约测试锁定。

---

## 13. UI计划

### 13.1 Windows主控台

主要页面：

1. **仪表盘**：挂载、播放器、Web服务、当前房间和错误状态；
2. **正在播放**：歌曲、进度、音轨、字幕和控制按钮；
3. **点歌队列**：拖动排序、置顶、删除、插播；
4. **曲库搜索**：快速搜索、筛选、查看详细信息；
5. **曲库管理**：媒体源、扫描进度、错误和人工修正；
6. **房间**：二维码、在线访客、点歌规则；
7. **历史与收藏**；
8. **设置**：播放器、显示器、音频、端口、数据库、日志；
9. **诊断**：导出诊断包、查看最近错误。

### 13.2 手机端

主要页面：

1. 加入房间；
2. 首页/热门/最近新增；
3. 搜歌；
4. 歌手详情；
5. 当前播放与待唱队列；
6. 我的点歌；
7. 收藏；
8. 简单设置。

要求：

- 单手操作；
- 主要按钮触控面积足够；
- 弱网或短暂断线有明确状态；
- 不因刷新页面而丢失昵称和当前房间；
- 中文为默认语言；
- V1.0至少验证 Chrome Android、Safari iPhone 和 Windows Chrome。

---

## 14. 非功能性要求

### 14.1 性能

- 应用启动到可操作：不含首次扫描，目标小于10秒；
- 100,000～300,000首索引搜索：P95小于200ms；
- 手机点歌到所有客户端看到更新：正常局域网内小于500ms；
- 本地媒体切歌目标小于5秒；远端115挂载的目标由实测基线确定；
- 扫描不能阻塞播放与点歌；
- 默认内存目标小于1.5GB，不计算mpv解码占用；
- 播放过程中禁止进行高并发全库ffprobe。

### 14.2 可靠性

- SQLite写操作使用事务；
- 数据库迁移前自动备份；
- 应用异常退出不破坏队列和曲库索引；
- 扫描与播放任务支持取消和超时；
- 所有后台任务异常必须被捕获并记录；
- 外部进程被终止后可以检测；
- 重要状态变化具有幂等处理。

### 14.3 安全

- 默认仅监听配置的局域网接口；
- 不默认暴露公网；
- 主持人操作需要独立凭据；
- 手机令牌具有房间范围和有效期；
- API不返回真实媒体路径；
- 日志不记录密码、令牌或完整认证数据；
- 对路径、昵称、搜索和排序输入进行验证；
- 对点歌和搜索接口设置合理频率限制；
- 第三方依赖执行许可证和漏洞检查。

### 14.4 可维护性

- 开启 nullable；
- 警告策略由仓库统一配置；
- 公共接口与复杂规则有XML或Markdown说明；
- 关键领域规则具有单元测试；
- 禁止把业务逻辑写进 WPF code-behind 或 API endpoint；
- 禁止以静态全局变量充当权威队列；
- 不允许吞掉异常或用“catch后继续”伪装成功。

---

## 15. 开发阶段与任务清单

任务编号采用 `KTVS-NNN`。每个任务原则上独立分支、独立提交/PR、独立验收。

### Phase 0：仓库调查与架构定稿

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-001 | 新仓库初始化与基线调查 | README、AGENTS、项目状态文档 |
| KTVS-002 | 产品边界与领域词汇 | CHARTER、术语表、用例 |
| KTVS-003 | 技术可行性Spike | WPF内嵌Host、SignalR、mpv IPC小样 |
| KTVS-004 | 架构决策记录 | ADR：仓库边界、播放器、数据库、Web构建、认证 |
| KTVS-005 | 实际环境基线 | Windows、挂载路径、样本MKV、音轨/字幕测试报告 |

**阶段门禁：** 至少用一首真实 MKV 完成“C#启动mpv、读取状态、切换音轨、接收结束事件”；未经验证不能进入大规模UI开发。

### Phase 1：工程骨架与持续集成

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-006 | 建立解决方案和项目依赖方向 | Domain/Application/Infrastructure/Server/Desktop/Web |
| KTVS-007 | 配置、日志和错误模型 | 强类型配置、日志轮转、统一结果类型 |
| KTVS-008 | 测试基线 | xUnit项目、测试数据工厂、覆盖率报告 |
| KTVS-009 | CI基线 | restore/build/test/web build/格式检查 |
| KTVS-010 | 本地开发脚本 | bootstrap、build、test、run命令 |

**阶段门禁：** 全新克隆可通过一条文档化命令构建；CI通过；没有提交密钥和本机绝对路径。

### Phase 2：数据库、扫描与搜索

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-011 | 领域模型与SQLite Schema | 首版实体、EF迁移 |
| KTVS-012 | MediaSource管理 | 多根目录、启停、路径验证 |
| KTVS-013 | 文件枚举与增量扫描 | 可取消、可恢复、错误隔离 |
| KTVS-014 | 文件名解析器 | 真实文件名样本测试集 |
| KTVS-015 | ffprobe媒体探测 | 时长、轨道、编码信息 |
| KTVS-016 | NFO可选读取 | 不一致处理、人工字段优先级 |
| KTVS-017 | 搜索规范化 | 拼音、简拼、原文索引字段 |
| KTVS-018 | FTS搜索与筛选 | 分页API、排序和性能测试 |
| KTVS-019 | 扫描管理API | 创建、取消、进度、结果 |

**阶段门禁：** 对脱敏后的真实曲库样本进行扫描；解析错误有报告；100,000条合成数据搜索达到性能目标；扫描不修改媒体文件。

### Phase 3：播放器与播放状态机

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-020 | `IPlayerAdapter`契约 | 状态、命令、事件、错误定义 |
| KTVS-021 | mpv进程与IPC | 启动、超时、响应关联、事件 |
| KTVS-022 | 播放状态机 | Idle/Preparing/Playing/Paused/Failed |
| KTVS-023 | 音量、进度和字幕控制 | 命令与状态同步 |
| KTVS-024 | 原唱/伴奏轨道映射 | 自动候选、人工修正、持久化 |
| KTVS-025 | 播放故障恢复 | 文件离线、403、超时、mpv崩溃 |
| KTVS-026 | 连续播放耐久测试 | 50首连续播放报告 |

**阶段门禁：** 真实双音轨MKV可稳定切换；播放完成只触发一次；错误文件不会卡死；50首耐久测试通过或形成明确问题清单。

### Phase 4：房间与队列后端

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-027 | 房间生命周期 | 创建、关闭、恢复 |
| KTVS-028 | 访客与主持人认证 | 短期令牌和权限策略 |
| KTVS-029 | 队列领域规则 | 点歌、限额、删除、置顶、排序 |
| KTVS-030 | 队列与播放器编排 | 准备、播放、完成、失败、下一首 |
| KTVS-031 | REST API与OpenAPI | 统一错误、验证、分页 |
| KTVS-032 | SignalR完整快照与增量事件 | 重连恢复、版本控制 |
| KTVS-033 | 历史、收藏和热门统计 | 数据模型与接口 |

**阶段门禁：** 并发点歌测试无重复顺序；权限测试通过；断线重连能恢复完整队列；服务重启不破坏数据。

### Phase 5：手机点歌Web

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-034 | React/Vite基础工程 | 构建、路由、API客户端、状态模型 |
| KTVS-035 | 加入房间与会话恢复 | 昵称、令牌、安全存储 |
| KTVS-036 | 搜歌与筛选 | 防抖、分页、空状态和错误状态 |
| KTVS-037 | 点歌和我的歌曲 | 限额提示、删除、重复提示 |
| KTVS-038 | 当前播放和实时队列 | SignalR重连、快照恢复 |
| KTVS-039 | 收藏、热门、最近新增 | 移动端交互 |
| KTVS-040 | 响应式与兼容性 | Android Chrome、iPhone Safari |

**阶段门禁：** 5台手机并发加入和点歌；刷新/短断网后恢复；手机端无真实路径或管理员令牌泄漏。

### Phase 6：Windows主控台

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-041 | WPF外壳与导航 | MVVM、依赖注入、主题 |
| KTVS-042 | 仪表盘与健康状态 | 服务、挂载、mpv、数据库 |
| KTVS-043 | 正在播放控制台 | 播放控制、音轨、字幕、进度 |
| KTVS-044 | 队列管理 | 拖动、置顶、删除、插播 |
| KTVS-045 | 曲库管理 | 搜索、详情、扫描、人工修正 |
| KTVS-046 | 房间与二维码 | 开关房间、访客、规则 |
| KTVS-047 | 设置与诊断 | 路径、端口、播放器、日志导出 |

**阶段门禁：** 普通用户不用命令行即可完成首次配置、扫描、开房和播放；错误提示包含可执行的恢复建议。

### Phase 7：集成、性能和可靠性

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-048 | 端到端测试 | 扫描→搜索→点歌→播放→完成 |
| KTVS-049 | 大曲库性能测试 | 100k/300k数据，搜索与扫描基线 |
| KTVS-050 | CloudDrive故障注入 | 断挂载、超时、403、恢复 |
| KTVS-051 | 进程和断电恢复 | mpv崩溃、应用重启、DB恢复 |
| KTVS-052 | 安全审查 | 局域网绑定、令牌、日志、路径 |
| KTVS-053 | 依赖与许可证审查 | mpv/FFmpeg/前后端依赖清单 |
| KTVS-054 | 用户验收测试包 | 检查表、样例房间和诊断说明 |

**阶段门禁：** 关键场景自动化通过；实际设备联调完成；没有P0/P1缺陷；P2缺陷有记录和计划。

### Phase 8：发布V1.0

| 任务 | 内容 | 主要产物 |
| --- | --- | --- |
| KTVS-055 | Windows发布与安装 | 版本化安装包或自包含发布包 |
| KTVS-056 | 升级和数据库迁移 | 迁移前备份、失败回滚说明 |
| KTVS-057 | 运维文档 | 安装、配置、备份、恢复、故障排除 |
| KTVS-058 | V1.0候选版验证 | 干净机器安装、实机验收 |
| KTVS-059 | 正式发布 | Tag、Release Notes、校验值 |

**阶段门禁：** 干净Windows环境可按文档完成安装；升级不会丢失数据库；Release产物可复现。

---

## 16. 测试计划

### 16.1 单元测试

重点覆盖：

- 文件名解析；
- Unicode和特殊字符；
- 队列排序与并发规则；
- 权限判断；
- 播放状态迁移；
- 搜索规范化；
- 音轨映射优先级；
- 重试和错误分类。

### 16.2 集成测试

- SQLite迁移和事务；
- 文件扫描与离线标记；
- ffprobe输出解析；
- ASP.NET API和认证；
- SignalR连接、断线和重连；
- mpv IPC使用可控进程或协议测试替身；
- Web静态文件由服务端正确提供。

### 16.3 端到端测试

- 新建数据库并配置样本目录；
- 扫描并搜索真实样本；
- 手机加入房间；
- 多人点歌；
- 主机播放并自动下一首；
- 音轨/字幕切换；
- 文件不存在或加载失败；
- 应用重启后恢复。

### 16.4 实机验收

必须由用户协助验证的项目：

- Windows电脑到电视/投影仪的画面；
- 实际功放和音频设备选择；
- 双音轨是否符合原唱/伴奏语义；
- 日本家庭局域网内手机扫码和连接；
- CloudDrive挂载115的真实加载速度；
- 连续聚会使用中的操作体验。

Codex必须提供清晰步骤和结果记录模板，不能把“需要实机确认”写成“测试通过”。

---

## 17. Git和任务工作流

### 17.1 分支

建议：

```text
main      正式发布
develop   已集成、可运行开发版本
task-KTVS-NNN-short-description
```

如果仓库已有不同治理规则，以仓库现有 `AGENTS.md` 和文档为准，不擅自改写历史。

### 17.2 每个任务的完成条件

任务只有同时满足以下条件才可标记完成：

- 需求和边界明确；
- 实现完成；
- 必需测试已添加并通过；
- 文档同步更新；
- 构建、后端测试、前端测试和格式检查通过；
- 无无关文件和生成垃圾；
- 工作树状态清楚；
- 提交信息包含任务号；
- `STATUS.md`、`BACKLOG.md` 和必要ADR已更新；
- 最终报告列出验证命令、结果、未解决风险和下一任务。

### 17.3 提交规范

示例：

```text
feat(KTVS-014): parse structured KTV media filenames
fix(KTVS-025): recover queue after remote media timeout
test(KTVS-029): cover concurrent queue ordering
docs(KTVS-057): add database recovery runbook
```

禁止：

- 把多个无关任务塞入一次提交；
- 未测试就写“已完成”；
- 重写用户未授权的提交历史；
- 删除或覆盖用户的现有修改；
- 提交密码、Token、真实个人数据或完整本机路径配置。

---

## 18. Codex自主工作规则

### 18.1 Codex可以自主完成

- 调查仓库和运行环境；
- 建立任务和技术方案；
- 创建和修改代码、测试与文档；
- 选择小型、维护活跃且许可证兼容的依赖；
- 执行构建、测试、静态检查和本地非破坏性验证；
- 创建任务分支和本地提交；
- 修复测试失败；
- 在既定边界内重构；
- 更新状态、决策、风险和下一步；
- 在没有重大歧义时选择最小、可逆、低耦合方案。

### 18.2 必须暂停并请求用户确认

- 删除、移动、重命名真实KTV媒体；
- 改变或清理115、百度网盘、CloudDrive上的文件；
- 输入或导出账号密码、Token、Cookie；
- 向公网开放服务、路由器端口或防火墙；
- 购买软件、服务或许可证；
- 操作真实GitHub设置、合并受保护分支或发布外部版本，除非用户已明确授权；
- 需要用户在电视、功放、手机或真实网络上确认结果；
- 发现现有用户修改与任务直接冲突；
- 架构选择会破坏本文确定的项目边界；
- 许可证结论不清楚且准备分发二进制。

### 18.3 永久禁止

- 为了“自动化”绕过权限；
- 用删除媒体来修复索引问题；
- 将测试替身结果描述为真实硬件通过；
- 隐藏失败、静默吞异常或伪造日志；
- 未经确认修改 AI-KTV-Builder 的领域边界；
- 在手机API中暴露实际115或Windows文件路径；
- 因为远端暂时不可用就删除歌曲数据库记录。

---

## 19. 跨会话连续工作机制

Codex每次开始工作必须按顺序读取：

1. 根目录 `AGENTS.md`；
2. `README.md`；
3. `docs/project/CHARTER.md`；
4. `docs/project/STATUS.md`；
5. `docs/project/BACKLOG.md`；
6. `docs/project/DECISIONS.md`；
7. 当前任务对应文档、ADR和测试。

### 19.1 STATUS.md至少包含

- 当前分支和HEAD；
- 当前版本；
- 已完成阶段和任务；
- 当前任务；
- 最近验证结果；
- 已知问题；
- 外部阻塞；
- 下一条推荐命令或任务；
- 用户需要执行的实机验证。

### 19.2 BACKLOG.md至少包含

- 任务ID；
- 标题；
- 依赖；
- 状态；
- 验收标准；
- 风险；
- 是否需要用户参与。

### 19.3 DECISIONS.md与ADR

- 小型可逆决定写入 `DECISIONS.md`；
- 跨模块、难以逆转或影响长期维护的决定写ADR；
- ADR状态使用 Proposed/Accepted/Superseded；
- 新实现不得暗中违背已接受ADR。

---

## 20. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 115挂载延迟或403 | 切歌慢、播放失败 | 超时、一次重试、自动跳过、健康状态、下一首预检 |
| 数十万曲目扫描慢 | 初次建库耗时 | 增量扫描、检查点、低并发、只探测变化文件 |
| 远端全文件哈希代价高 | 大量读取和限流 | 默认路径+大小+时间，必要时首尾分块指纹 |
| 文件名不统一 | 搜索信息错误 | 容错解析、置信度、人工修正、样本回归测试 |
| 音轨顺序不一致 | 原唱/伴奏选错 | 探测、映射、人工修正优先、批量操作先预览 |
| mpv或FFmpeg分发许可 | 发布风险 | 依赖与许可证ADR；必要时只检测用户安装路径 |
| 手机断线 | 队列显示过期 | 重连后获取完整快照和版本号 |
| 多人并发点歌 | 顺序冲突 | 服务端权威排序、事务、并发测试 |
| Windows异常退出 | 队列/数据丢失 | SQLite事务、状态持久化、启动恢复 |
| 需求膨胀 | 长期不能发布 | 严格V1范围、阶段门禁、V1.1候选清单 |
| AI项目强耦合 | 两个项目互相阻塞 | 文件/API显式边界、分仓库、独立发布 |

---

## 21. 里程碑和预估

在Codex持续开发、用户能及时完成少量实机确认的条件下：

| 里程碑 | 结果 | 粗略周期 |
| --- | --- | --- |
| M0 技术验证 | C#控制mpv播放真实MKV | 2～4天 |
| M1 可搜索曲库 | 扫描、SQLite、搜索API | 1～2周 |
| M2 Windows可点歌 | 队列、播放控制、自动下一首 | 1～2周 |
| M3 手机点歌 | 扫码、多人实时队列 | 1～2周 |
| M4 稳定化 | 115容错、性能、恢复、诊断 | 1～2周 |
| M5 V1.0 | 安装包、文档、实机验收 | 约1周 |

总计约5～8周。Codex可减少编码和测试编写时间，但真实硬件、CloudDrive行为、音轨差异和大曲库性能仍需要分阶段验证。

---

## 22. 交给Codex的主提示词

将下面整段复制到目标仓库中的 Codex。建议新仓库路径为 `D:\AI-KTV-Station`；如果该目录已经存在，必须先调查，不得覆盖。

```text
你是 AI-KTV Station 项目的首席架构师、开发工程师、测试工程师和技术文档负责人。请在授权范围内持续推进项目，不要只给建议或示例代码；要实际调查仓库、制定任务、实现、测试、更新文档并提交成果。

项目目标：开发一套 Windows 11 家庭KTV点歌系统。Windows主机负责曲库管理和播放，朋友通过同一局域网的手机浏览器扫码点歌。曲库主要位于115网盘，并通过CloudDrive/CloudDrive2挂载到Windows。播放器读取MKV，必须支持原唱/伴奏音轨切换、字幕、队列、自动下一首和播放故障恢复。

确定技术栈：
- C# / .NET 10；
- WPF + MVVM作为Windows主控；
- ASP.NET Core Minimal API + SignalR作为内嵌服务；
- React + TypeScript + Vite作为手机端；
- EF Core + SQLite作为本地索引数据库；
- mpv子进程 + JSON IPC作为首选播放引擎；
- ffprobe/FFmpeg负责媒体探测；
- xUnit等兼容测试工具负责自动化验证。

强制架构边界：
1. 本项目是独立的 AI-KTV Station 仓库，不把现有 AI-KTV-Builder 合并进来。
2. AI-KTV-Builder继续负责制歌；Station只负责索引、点歌、队列和播放。
3. 首版Web API不转发视频，mpv直接读取挂载路径。
4. 手机端不得获取Windows真实文件路径、云盘凭据或永久管理员密钥。
5. 扫描器只读媒体。未经用户明确确认，绝不删除、移动、重命名或覆盖真实媒体。
6. 领域层不得依赖WPF、ASP.NET、EF Core、mpv或文件系统。
7. 播放器必须通过IPlayerAdapter抽象，业务层不得直接散落mpv IPC代码。
8. 远端文件暂时不可用时只标记状态和记录错误，不删除歌曲索引。
9. 不得伪造成功；需要电视、功放、手机或真实115挂载验证时，明确输出用户验收步骤并标记为待验证。

权威开发计划位于仓库的《AI-KTV Station 完整开发计划书与 Codex 执行提示词》。开始前完整读取它以及仓库内所有AGENTS.md和项目状态文档。

你的工作方式：
1. 先执行只读调查：当前目录、Git状态、已有文件、工具版本、可用.NET/Node/mpv/FFmpeg环境。保留用户已有修改。
2. 如果是空目录，初始化独立仓库和计划规定的最小项目结构；如果不是空目录，先报告现状和冲突，禁止覆盖。
3. 创建并维护：docs/project/CHARTER.md、STATUS.md、BACKLOG.md、DECISIONS.md、TEST-MATRIX.md、RELEASE-CHECKLIST.md，以及必要ADR。
4. 将计划中的KTVS任务转为可执行backlog，保留依赖关系和阶段门禁。
5. 从KTVS-001开始，一次只推进一个边界清楚的任务。每个任务必须包含实现、测试、文档、验证和状态更新。
6. 重大架构实现前先完成ADR；可逆的小决定自行选择最小、低耦合方案并记录。
7. 每次修改后运行与改动相关的最小测试；任务结束前运行完整的适用构建/测试/格式检查。
8. 不要因为局部环境缺少某个工具就绕过测试或伪造结果。记录阻塞，并继续完成不依赖该工具的安全工作。
9. 每个完成任务使用包含任务号的提交信息。未经授权不要推送、创建PR、合并或发布；可以完成本地分支和提交。
10. 每次工作结束必须给出：完成内容、修改文件、验证命令和结果、Git状态、风险/阻塞、需要用户实机确认的项目、下一推荐任务。

自主权限：可以调查、建仓、写代码、测试、重构、选取许可证兼容的小型依赖、运行非破坏性命令、创建分支和本地提交、维护文档。

停止条件：删除/移动真实媒体；处理账号密码/Token/Cookie；公网开放；购买服务；外部发布/合并；现有用户改动冲突；需要真实电视/功放/手机/115验证；不清楚的二进制分发许可证。遇到这些情况时先完成所有不受阻塞的工作，再提出一个具体、最少量的问题。

现在开始执行 Phase 0。不要一次实现整个系统。先完成仓库调查、项目状态文档、backlog和KTVS-001；然后在同一轮中，如果没有阻塞，继续KTVS-002。到需要真实MKV或Windows设备验证前再停。请实际执行，不要仅复述计划。
```

---

## 23. 后续继续开发提示词

新会话中使用以下提示词，不需要重新解释整个项目：

```text
继续 AI-KTV Station 项目。先完整读取所有适用AGENTS.md，以及 README.md、docs/project/CHARTER.md、STATUS.md、BACKLOG.md、DECISIONS.md 和当前任务相关ADR/测试。

先用只读命令核对当前分支、HEAD、工作树、最近提交和STATUS记录是否一致。不要覆盖或丢弃现有修改。如果状态一致，选择BACKLOG中依赖已满足、优先级最高的下一个KTVS任务并实际完成：实现、测试、文档、验证、状态更新和本地提交。

继续遵守媒体只读、Station与AI-KTV-Builder分离、手机不暴露真实路径、不得伪造实机测试、未经授权不推送/不合并/不发布等边界。

除非触及明确停止条件，不要询问我应该做什么；自行选择最小、可逆、低耦合方案并记录决定。完成后报告结果和下一任务。
```

---

## 24. Phase 0专用提示词

如果希望先单独完成技术验证，可使用：

```text
执行 AI-KTV Station 的 Phase 0，仅处理 KTVS-001 至 KTVS-005，不进入正式功能UI开发。

目标是证明关键链路可行：WPF/.NET Generic Host可以启动内嵌ASP.NET Core服务；手机浏览器可访问一个最小局域网页；C#可以启动mpv并通过JSON IPC加载一首真实双音轨MKV、读取播放状态、切换音轨/字幕并收到播放结束事件；ffprobe可以读取轨道信息。

先完成仓库和环境调查、状态文档、backlog和ADR。创建最小Spike时保持代码可删除或可演进，不要提前构建庞大框架。对需要我提供真实MKV、挂载路径、电视或音频设备验证的部分，给出一条一条可执行的验收步骤，未实测部分必须标记待验证。

禁止修改、移动、重命名或删除任何媒体。完成后运行可执行的自动测试，更新STATUS并提交Phase 0成果。
```

---

## 25. 代码审查提示词

可在每个阶段结束时开一个新的 Codex 会话执行独立审查：

```text
对 AI-KTV Station 当前阶段做只读优先的独立架构和代码审查。读取开发计划、AGENTS.md、ADR、STATUS、BACKLOG、测试矩阵和本阶段全部提交。

重点检查：
- 是否违反Domain/Application/Infrastructure依赖方向；
- 是否把业务逻辑写入WPF code-behind或API endpoint；
- 是否绕过IPlayerAdapter直接调用mpv；
- 队列并发、状态迁移和事件是否幂等；
- 扫描是否可能修改真实媒体；
- 手机API是否泄露路径、令牌或敏感配置；
- SignalR断线后是否可恢复完整状态；
- SQLite事务、迁移、备份和异常恢复；
- CloudDrive离线、403和超时是否会卡死队列；
- 测试是否真正覆盖风险而不是只追求覆盖率；
- 文档是否与实现一致。

本轮默认只诊断，不修改代码。按严重度列出问题，提供文件和具体证据，区分事实与推断，并给出建议修复任务编号。如果没有证据，不要宣称通过。
```

---

## 26. 用户在开发期间需要准备的内容

Codex可以负责绝大多数开发，但以下实物信息需要用户在相应阶段提供：

1. 新仓库实际路径，例如 `D:\AI-KTV-Station`；
2. 一小组脱敏测试媒体：
   - 正常双音轨MKV；
   - 带字幕MKV；
   - 单音轨MKV；
   - 文件名较复杂的中文、英文、日文歌曲；
   - 一个故意损坏或不可访问的样本；
3. Windows上的CloudDrive实际挂载根目录；
4. mpv和FFmpeg是否允许随程序提供，或只引用用户自行安装版本；
5. 电视/投影仪和音频设备实测结果；
6. 手机扫码时主机所在局域网地址和防火墙测试；
7. 每个阶段用户验收清单的通过/失败反馈。

不要向Codex提供115、百度网盘或其他账号密码。程序只读取已经由CloudDrive挂载好的文件系统路径。

---

## 27. 推荐的第一步

1. 在Windows创建空目录 `D:\AI-KTV-Station`；
2. 把本计划书复制到该目录；
3. 在该目录打开Codex；
4. 粘贴第22节“交给Codex的主提示词”；
5. 允许Codex先完成Phase 0；
6. 当Codex请求真实MKV验证时，只提供几首测试歌曲，不要直接用整个12TB曲库；
7. Phase 0通过后，再让Codex按BACKLOG持续推进。

在技术Spike验证成功之前，不应投入大量手机UI和曲库管理界面开发。最先确认的不是页面外观，而是“真实MKV、mpv IPC、双音轨、字幕、远端挂载和播放结束事件”这一条最关键链路。

