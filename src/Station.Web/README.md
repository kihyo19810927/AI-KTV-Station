# Station.Web

React + TypeScript + Vite 手机点歌端。开发使用 `npm.cmd run dev`，生产构建输出到 `src/Station.Server/wwwroot` 并由 ASP.NET Core 托管。

命令：`npm.cmd ci`、`npm.cmd run typecheck`、`npm.cmd test`、`npm.cmd run build`。
